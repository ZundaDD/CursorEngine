<UserControl x:Class="CursorEngine.View.BrowserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CursorEngine.Frontend.View"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:v="clr-namespace:CursorEngine.View"
             xmlns:vm="clr-namespace:CursorEngine.ViewModel"
             xmlns:ser="clr-namespace:CursorEngine.Services"
             mc:Ignorable="d" 
             d:DesignHeight="600" 
             d:DesignWidth="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Grid Grid.Row="0">
            <StatusBar Margin="5">
                <StatusBar.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </ItemsPanelTemplate>
                </StatusBar.ItemsPanel>

                <StatusBarItem Grid.Column="0">
                    <Button Command="{Binding DataContext.NavigateToLocalSchemesCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    Style="{StaticResource MaterialDesignIconButton}"
                    VerticalAlignment="Center"
                    Height="25"
                    Width="30">
                        <md:PackIcon Kind="Local"/>
                    </Button>
                </StatusBarItem>

                <StatusBarItem Grid.Column="1" HorizontalAlignment="Center">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                Command="{Binding GoPreviousCommand}"
                                ToolTip="上一页"
                                Width="30" Height="25">
                            <md:PackIcon Kind="ChevronLeft" />
                        </Button>

                        <TextBlock Text="{Binding CurrentPageIndex}"
                                   VerticalAlignment="Center"
                                   Margin="10,0"
                                   Foreground="Black"/>

                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                Command="{Binding GoNextCommand}"
                                ToolTip="下一页"
                                Width="30" Height="25">
                            <md:PackIcon Kind="ChevronRight" />
                        </Button>

                    </StackPanel>
                </StatusBarItem>

                <StatusBarItem Grid.Column="2">
                    <Button Style="{StaticResource MaterialDesignIconButton}"
                            Height="25"
                            Width="30"
                            Command="{Binding DownloadSchemeCommand}"
                            ToolTip="下载方案">
                        <md:PackIcon Kind="Download" />
                    </Button>
                </StatusBarItem>
            </StatusBar>
        </Grid>

        <TextBlock Grid.Row="1"
                 HorizontalAlignment="Center"
                 Foreground="Aquamarine"
                 Text="{Binding StatusMessage}"
                 TextWrapping="Wrap"/>

        <ListBox ItemsSource="{Binding CurrentPage.Schemes}"
                 SelectedItem="{Binding SelectedScheme}"
                 Grid.Row="2"
                 Margin="7"
                 Background="Transparent"
                 HorizontalAlignment="Stretch">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>

                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="0"/>

                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Bd" 
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter />
                                </Border>

                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#22FFFFFF" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#44FFFFFF" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#666" 
                            BorderThickness="1" 
                            Padding="8" 
                            CornerRadius="5"
                            Margin="1"
                            Background="LightGray">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0"
                                        Orientation="Horizontal">
                                <Image Source="{Binding PreviewImage}" 
                                        Width="32"
                                        Height="32" 
                                        Stretch="Uniform"/>
                                <TextBlock Text="{Binding Name}" 
                                            FontSize="16" 
                                            Foreground="Black" 
                                            FontWeight="Bold"
                                            TextAlignment="Center"
                                            VerticalAlignment="Center"/>
                            </StackPanel>

                            <Button Grid.Column="1"
                                    VerticalAlignment="Center"
                                    Width="25"
                                    Height="25"
                                    Command="{Binding DataContext.ViewSchemeCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource MaterialDesignIconButton}">
                                <md:PackIcon Kind="Eye"/>
                            </Button>
                            
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
